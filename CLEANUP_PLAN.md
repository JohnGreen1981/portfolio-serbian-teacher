# Cleanup Plan

- [ ] Copy `bot.py`, `handlers/`, `services/`, `prompts/`, `keyboards.py`, `schema.sql`, `requirements.txt`.
- [ ] Exclude `.env`, existing `.env.example`, `__pycache__/`, `.mcp.json`, PDFs, docx passport, and local files.
- [ ] Create a safe `.env.example` from scratch.
- [ ] Remove or rewrite any copyrighted textbook-derived content if needed.
- [ ] Run secret scan before first GitHub push.

